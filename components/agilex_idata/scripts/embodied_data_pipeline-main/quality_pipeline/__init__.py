"""Robot data processing pipeline helpers.

The modules in this package are intentionally lightweight: they depend on the
standard library so the first QC/annotation pass can run inside the robot
workspace before heavier LeRobot conversion dependencies are installed.
"""

